
<p><%= page_entries_info @accounts, :model => 'Person' %></p>

<div class="row">
  <% @accounts.each { |account| %>
    <div class="<%= container_class %> account-minimal">
      <%= partial :'accounts/minimal', :locals => {:account => account}  %>
    </div>
  <% } %>
</div>

<%= will_paginate @accounts, :renderer => WillPaginate::ViewHelpers::BootstrapRenderer, :page_links => page_links %>